extends ../../../components/main_layout/templates/index

append locals
  - assetPackage = 'home'
  - bodyClass = 'home'

block head
  title Takoman

block body
  .container
    .brand
      include ../../../components/main_layout/public/svgs/takoman.svg

    ol.breadcrumb
      li: a#login( href="#" data-active-tab="login" ) 登入
      li: a#signup( href="#" data-active-tab="signup" ) 註冊
      if user
        li: a#logout( href="/logout" ) 登出 (#{user.get('email')})
